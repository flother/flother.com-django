Entry:    "{{ entry.title }}", {{ comment.site.name }}
Name:     {{ comment.userinfo.name }}
Email:    {{ comment.userinfo.email|default:"[Not given]" }}
URL:      {{ comment.userinfo.url|default:"[Not given]" }}

{{ comment.comment|wordwrap:72 }}

This comment is publicly visible on the site as Akismet doesn't think
it's spam.

-- 
{{ comment.site.name }}
<http://{{ comment.site.domain }}/>
